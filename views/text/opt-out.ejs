<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SMS Opt-Out - Sister Missionary Meal Calendar</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <%- include('../partials/navigation', { activePage: 'text' }) %>
    <%- include('../partials/sms-notice') %>

    <div class="container mt-4">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header bg-warning text-dark">
                        <h4 class="mb-0">🚫 SMS Opt-Out</h4>
                    </div>
                    <div class="card-body">
                        <% if (success) { %>
                            <div class="alert alert-success">
                                <i class="fas fa-check-circle"></i> <%= success %>
                                <hr>
                                <p class="mb-0">
                                    <strong>What happens next:</strong><br>
                                    • You will no longer receive SMS notifications<br>
                                    • Important meal information will be sent via email instead<br>
                                    • You can opt back in at any time by visiting the opt-in page
                                </p>
                            </div>
                            <div class="text-center">
                                <a href="/text/opt-in" class="btn btn-success">Opt Back In</a>
                                <a href="/dashboard" class="btn btn-primary">Return to Dashboard</a>
                            </div>
                        <% } else { %>
                            <% if (error) { %>
                                <div class="alert alert-danger">
                                    <i class="fas fa-exclamation-triangle"></i> <%= error %>
                                </div>
                            <% } %>

                            <div class="alert alert-warning">
                                <h5 class="alert-heading">Are you sure you want to opt out?</h5>
                                <p>If you opt out of SMS notifications, you will:</p>
                                <ul>
                                    <li>No longer receive text message reminders about meal assignments</li>
                                    <li>Miss important updates about schedule changes</li>
                                    <li>Not be notified about volunteer opportunities</li>
                                </ul>
                                <hr>
                                <p class="mb-0">
                                    <strong>Alternative:</strong> We will continue to send important information via email.
                                </p>
                            </div>

                            <form method="POST" action="/text/opt-out">
                                <div class="mb-3">
                                    <label for="phoneNumber" class="form-label">Phone Number</label>
                                    <input type="tel" class="form-control" id="phoneNumber" name="phoneNumber" 
                                           value="<%= phoneNumber %>" required
                                           placeholder="(555) 123-4567">
                                    <div class="form-text">Enter the phone number you want to remove from SMS notifications</div>
                                </div>

                                <div class="mb-3">
                                    <div class="form-check">
                                        <input class="form-check-input" type="checkbox" id="confirm" required>
                                        <label class="form-check-label" for="confirm">
                                            I confirm that I want to opt out of SMS notifications for meal coordination.
                                        </label>
                                    </div>
                                </div>

                                <div class="d-grid gap-2">
                                    <button type="submit" class="btn btn-warning btn-lg">
                                        🚫 Opt Out of SMS Notifications
                                    </button>
                                </div>
                            </form>

                            <hr class="my-4">

                            <div class="text-center">
                                <p class="text-muted mb-2">Changed your mind?</p>
                                <a href="/text/opt-in" class="btn btn-outline-success">
                                    Stay Opted In to SMS
                                </a>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <footer class="bg-light text-center py-3 mt-5">
        <div class="container">
            <p class="text-muted mb-0">Sister Missionary Meal Calendar - Ward Meal Coordination System</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
