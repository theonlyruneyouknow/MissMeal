<!-- SMS Development Status Notice Component -->
<div id="sms-notice" class="alert alert-info" style="background-color: #e3f2fd; border: 1px solid #64b5f6; border-radius: 8px; padding: 1.5rem; margin-bottom: 2rem; position: relative;">
    <!-- Close button -->
    <button type="button" onclick="dismissSmsNotice()" style="position: absolute; top: 10px; right: 15px; background: none; border: none; font-size: 1.2rem; color: #1565c0; cursor: pointer; opacity: 0.7; transition: opacity 0.2s;" title="Hide this notice">
        <i class="fas fa-times"></i>
    </button>
    
    <div style="display: flex; align-items: flex-start; gap: 12px; padding-right: 30px;">
        <i class="fas fa-mobile-alt" style="color: #1565c0; font-size: 1.2rem; margin-top: 2px;"></i>
        <div>
            <h4 style="margin: 0 0 0.5rem 0; color: #1565c0; font-size: 1.1rem;">
                <strong>SMS Opt-In System</strong>
            </h4>
            <p style="margin: 0 0 0.5rem 0; color: #0d47a1; line-height: 1.5;">
                <strong>Users can now opt-in to receive SMS notifications when our automated system goes live.</strong>
            </p>
            <p style="margin: 0 0 0.5rem 0; color: #0d47a1; line-height: 1.5;">
                <strong>Important Notice:</strong> We are currently working on implementing a fully compliant SMS notification system 
                that meets all telecommunications regulations and privacy requirements.
            </p>
            <p style="margin: 0 0 1rem 0; color: #0d47a1; line-height: 1.5;">
                <strong>Current Status:</strong> While families can opt-in to SMS notifications through this system, 
                the automated text messaging is still in development.
            </p>
            
            <div style="background-color: rgba(13, 71, 161, 0.1); padding: 1rem; border-radius: 6px;">
                <p style="margin: 0 0 0.5rem 0; color: #0d47a1; font-weight: 600;">
                    <strong>Temporary Process:</strong>
                </p>
                <ul style="margin: 0.5rem 0 0 1.5rem; color: #0d47a1; line-height: 1.4;">
                    <li><strong>When families sign up for meals:</strong> They must manually text the sister missionaries with meal details (date, time, delivery preference, contact info)</li>
                    <li>Sister missionaries will be notified about meal assignments <strong>manually via text message</strong> for now</li>
                    <li>This opt-in system is collecting consent for when automated messaging goes live</li>
                </ul>
            </div>
        </div>
    </div>
</div>

<script>
function dismissSmsNotice() {
    const notice = document.getElementById('sms-notice');
    if (notice) {
        notice.style.transition = 'opacity 0.3s ease, height 0.3s ease, margin 0.3s ease, padding 0.3s ease';
        notice.style.opacity = '0';
        notice.style.height = '0';
        notice.style.marginBottom = '0';
        notice.style.paddingTop = '0';
        notice.style.paddingBottom = '0';
        setTimeout(() => {
            notice.style.display = 'none';
        }, 300);
        
        // Remember user's preference (optional - stores in localStorage)
        localStorage.setItem('sms-notice-dismissed', 'true');
    }
}

// Check if user previously dismissed the notice
document.addEventListener('DOMContentLoaded', function() {
    if (localStorage.getItem('sms-notice-dismissed') === 'true') {
        const notice = document.getElementById('sms-notice');
        if (notice) {
            notice.style.display = 'none';
        }
    }
});
</script>
